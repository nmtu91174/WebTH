@model WebTH.Models.EF.Promotion
@{
    ViewBag.Title = "Cập nhật khuyến mại";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
<script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

<style>
    .select2-container--default .select2-selection__choice {
        color: #000 !important; /* Ép chữ luôn màu đen */
        background-color: #e0e0e0;
        border: 1px solid #ccc;
        max-width: 130px;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
        font-size: 13px;
    }
</style>

@section naviheader {
    <!-- Left navbar links -->
    <ul class="navbar-nav">
        <li class="nav-item">
            <a class="nav-link" data-widget="pushmenu" href="#" role="button"><i class="fas fa-bars"></i></a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/home" class="nav-link">Trang chủ</a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="/admin/promotion" class="nav-link">Quản lý khuyến mại </a>
        </li>
        <li class="nav-item d-none d-sm-inline-block">
            <a href="#" class="nav-link">Sửa khuyến mại</a>
        </li>
    </ul>
}

<!-- Content Header (Page header) -->
<section class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1>Sửa khuyến mại</h1>
            </div>
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Khuyến mại</a></li>
                    <li class="breadcrumb-item active">Sửa</li>
                </ol>
            </div>
        </div>
    </div><!-- /.container-fluid -->
</section>

<!-- Main content -->
<section class="content">

    <!-- Default box -->
    <div class="card">
        <div class="card-header">
            <h3 class="card-title">Thông tin sửa khuyến mại</h3>

        </div>
        <div class="card-body">
            <div class="bs-stepper-content">
                @using (Html.BeginForm("Edit", "Promotion", FormMethod.Post, new { }))
                {
                    @Html.AntiForgeryToken()
                    @Html.ValidationSummary(true)
                    @Html.HiddenFor(x => x.Id)
                    @Html.HiddenFor(x => x.CreatedDate)
                    <!-- your steps content here -->
                    <div id="logins-part" class="content active dstepper-block" role="tabpanel" aria-labelledby="logins-part-trigger">
                        <div class="form-group">
                            <label for="exampleInputEmail1">Tiêu đề</label>
                            @Html.TextBoxFor(x => x.Title, new { @class = "form-control", @placeholder = "Tiêu đề" })
                            @Html.ValidationMessageFor(x => x.Title, null, new { @class = "text-danger" })
                        </div>
                        <div class="form-group">
                            <label for="exampleInputEmail1">Alias</label>
                            @Html.TextBoxFor(x => x.Alias, new { @class = "form-control", @placeholder = "Alias" })
                            @Html.ValidationMessageFor(x => x.Alias, null, new { @class = "text-danger" })
                        </div>
                        <div class="form-group">
                            <label for="exampleInputEmail1">Phần trăm giảm</label>
                            @Html.TextBoxFor(x => x.DiscountPercent, new { @class = "form-control", @placeholder = "Alias" })
                            @Html.ValidationMessageFor(x => x.DiscountPercent, null, new { @class = "text-danger" })
                        </div>
                        <div class="form-group">
                            <label for="exampleInputPassword1">Mô tả</label>
                            @Html.TextAreaFor(x => x.Description, new { @class = "form-control" })
                        </div>
                        <div class="form-group">
                            <label>Chọn sản phẩm áp dụng khuyến mãi</label>
                            @Html.ListBoxFor(x => x.SelectedProductIds, ViewBag.Products as MultiSelectList, new { @class = "form-control select2", @style = "width: 100%;" })

                        </div>
                        <div class="form-group">
                            <button type="submit" class="btn btn-primary">Lưu</button>
                            <a href="/admin/promotion" class="btn btn-danger">Quay lại</a>
                        </div>
                    </div>

                }

            </div>
        </div>
        <!-- /.card-body -->
        <div class="card-footer">

        </div>
        <!-- /.card-footer-->
    </div>
    <!-- /.card -->

</section>
<!-- /.content -->
@section scripts {
    <script>
        $(document).ready(function () {
            const select = $('.select2');

            // Khởi tạo Select2
            select.select2({
                placeholder: "Chọn sản phẩm...",
                allowClear: true,
                width: '100%' // rất quan trọng để tránh bị co lại
            });

            // Đợi Select2 render xong rồi override CSS chính xác
            setTimeout(function () {
                $('.select2-container--default .select2-selection--multiple').css({
                    'display': 'flex',
                    'flex-wrap': 'wrap',
                    'gap': '4px',
                    'max-height': '100px',
                    'overflow-y': 'auto',
                    'align-items': 'center',
                    'padding': '6px 8px'
                });

                $('.select2-container--default .select2-selection__choice').css({
                    'color': '#000',
                    'max-width': '130px',
                    'overflow': 'hidden',
                    'text-overflow': 'ellipsis',
                    'white-space': 'nowrap',
                    'font-size': '13px',
                    'background-color': '#e0e0e0',
                    'border': '1px solid #ccc'
                });
            }, 200);
        });
    </script>
}